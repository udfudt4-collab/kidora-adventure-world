import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kidora.adventureworld',
  appName: 'Kidora Adventure World',
  webDir: 'dist'
};

export default config;
